article.help
  .container-fluid
    section.row-fluid.summary
      .summary.col-sm-3
        h3 DROP INDEX ON
        p.lead Drop a schema index
      .details.col-sm-9
        p
          | The&nbsp;
          code DROP INDEX ON
          |  clause will an index on all nodes that have a label.

        table.table-condensed.table-help
          tr
            th Reference:
            td
              code
                a(href='{{ neo4j.version | neo4jDeveloperDoc }}/cypher/#query-schema-index') schema indexes
              |  manual page
          tr
            th Related:
            td
              a(help-topic='create-index-on') :help CREATE INDEX ON
              | &nbsp;
              a(help-topic='schema') :help Schema
              | &nbsp;
              a(help-topic='cypher') :help Cypher
    section.row-fluid.example
      .col-sm-9.col-sm-offset-3
        figure
          pre.code.runnable.standalone-example.
            DROP INDEX ON :Person(name)
          figcaption Drop the index on the label Person and property name.
