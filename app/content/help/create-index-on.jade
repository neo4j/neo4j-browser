article.help
  .container-fluid
    section.row-fluid.summary
      .summary.col-sm-3
        h3 CREATE INDEX ON
        p.lead Index labeled nodes by property
      .details.col-sm-9
        p
          | The&nbsp;
          code CREATE INDEX ON
          |  clause will create and populate an index on a property for all nodes that have a label.

        table.table-condensed.table-help
          tr
            th Reference:
            td
              code
                a(href='{{ neo4j.version | neo4jDeveloperDoc }}/cypher/#query-schema-index') schema indexes
              |  manual page
          tr
            th Related:
            td
              a(help-topic='drop-index-on') :help DROP INDEX ON
              | &nbsp;
              a(help-topic='schema') :help Schema
              | &nbsp;
              a(help-topic='cypher') :help Cypher
    section.row-fluid.example
      .col-sm-9.col-sm-offset-3
        figure
          pre.code.runnable.standalone-example.
            CREATE INDEX ON :Person(name)
          figcaption Create an index on the label Person and property name.
